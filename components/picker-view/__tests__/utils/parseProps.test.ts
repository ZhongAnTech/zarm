import { normalState, isValueValid } from '../../utils/parseProps';

describe('picker-view', () => {
  describe('utils', () => {
    describe('isValueValid', () => {
      it('should return true if there is a false value', () => {
        isValueValid([]);
      });
    });
    describe('normalState', () => {
      it('should get normal state', () => {});
    });
  });
});
